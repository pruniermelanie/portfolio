<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Portfolio — Mélanie Prunier</title>
  <meta name="description" content="Portfolio de Mélanie Prunier — Illustration & édition, communication, théâtre, storyboard, animation." />
  <meta property="og:title" content="Portfolio — Mélanie Prunier" />
  <meta property="og:description" content="Illustration & édition • Communication • Théâtre • Storyboard • Animation" />
  <meta property="og:type" content="website" />
  <meta name="theme-color" content="#f5f4ee" />
  <link rel="preload" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" as="style" onload="this.rel='stylesheet'">
  <style>
    /* THÈME CLAIR inspiré des estampes : papier clair, bleus profonds, rouge vermillon */
    :root{
      --bg: #fafcfe;          /* papier bleuté très clair */
      --panel: #edf3f9;       /* cartes bleu pâle */
      --panel-2: #ffffff;     /* blanc pur pour formulaires */
      --accent: #e73b2f;      /* rouge vermillon énergique */
      --accent-2: #0b6aa9;    /* bleu profond */
      --ink: #092133;         /* bleu encre pour titres */
      --text: #111418;        /* texte principal */
      --muted: #546474;       /* texte secondaire (gris bleuté) */
      --ring: color-mix(in oklab, var(--accent) 32%, transparent);
      --radius: 20px;
      --shadow: 0 10px 30px rgba(9,33,51,.08);
    }

    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0; font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Apple Color Emoji","Segoe UI Emoji";
      background:
        radial-gradient(1000px 520px at 85% -10%, rgba(11,106,169,.10), transparent 60%),
        radial-gradient(900px 520px at -10% 15%, rgba(231,59,47,.10), transparent 55%),
        var(--bg);
      color:var(--text); line-height:1.6;
    }
    a{color:inherit; text-decoration:none}
    img, video{max-width:100%; display:block}

    /* Layout */
    .container{width:min(1150px, 92vw); margin-inline:auto;}
    header{
      position:sticky; top:0; z-index:50; backdrop-filter:saturate(140%) blur(10px);
      background: color-mix(in oklab, var(--bg) 80%, transparent);
      border-bottom:1px solid rgba(9,33,51,.08);
    }
    .nav{display:flex; align-items:center; justify-content:space-between; gap:16px; padding:14px 0}
    .brand{display:flex; align-items:center; gap:12px}
    .logo{width:40px; aspect-ratio:1/1; border-radius:12px; background:conic-gradient(from 210deg,var(--accent),var(--accent-2)); box-shadow:var(--shadow)}
    .brand h1{font-size:18px; letter-spacing:.3px; margin:0; color:var(--ink)}
    .navlinks{display:flex; flex-wrap:wrap; gap:6px}
    .pill{padding:8px 12px; border-radius:999px; font-size:14px; color:var(--muted); border:1px solid rgba(9,33,51,.10)}
    .pill:hover,.pill:focus{color:var(--ink); outline:none; border-color:rgba(9,33,51,.2); box-shadow:0 0 0 6px var(--ring)}

    .hero{padding:64px 0 28px}
    .hero-card{display:grid; grid-template-columns:1.15fr .85fr; gap:28px; padding:26px; border:1px solid rgba(9,33,51,.10); border-radius:26px; background:linear-gradient(180deg, rgba(255,255,255,.78), rgba(255,255,255,.55)); box-shadow:var(--shadow)}
    .eyebrow{color:var(--accent-2); font-weight:700; letter-spacing:.06em; text-transform:uppercase; font-size:12px}
    .hero h2{font-size: clamp(28px, 4.6vw, 46px); line-height:1.08; margin:6px 0 8px; color:var(--ink)}
    .hero p{color:var(--muted); margin:0}
    .cta-row{margin-top:18px; display:flex; gap:12px; flex-wrap:wrap}
    .btn{padding:12px 16px; border-radius:12px; border:1px solid rgba(9,33,51,.12); display:inline-flex; align-items:center; gap:10px; font-weight:700}
    .btn.primary{background:linear-gradient(135deg, color-mix(in oklab, var(--accent), white 10%), color-mix(in oklab, var(--accent-2), white 12%)); color:#fff}

    .tags{display:flex; gap:8px; flex-wrap:wrap; margin-top:12px}
    .tag{font-size:12px; color:var(--muted); padding:6px 10px; border-radius:999px; border:1px dashed rgba(9,33,51,.20)}

    section{padding:48px 0}
    .section-title{display:flex; align-items:flex-end; justify-content:space-between; gap:16px; margin-bottom:18px}
    .section-title h3{margin:0; font-size:clamp(18px, 2.6vw, 28px); color:var(--ink)}
    .section-title p{margin:0; color:var(--muted)}

    .grid{display:grid; grid-template-columns: repeat(12, 1fr); gap:14px}
    .card{grid-column: span 4; background:var(--panel); border:1px solid rgba(9,33,51,.1); border-radius:var(--radius); overflow:hidden; box-shadow:var(--shadow)}
    .card .thumb{aspect-ratio: 4/3; background:#f2f7fb; display:flex; align-items:center; justify-content:center}
    .card h4{font-size:16px; margin:10px 12px 0; color:var(--ink)}
    .card p{margin:6px 12px 12px; color:var(--muted); font-size:14px}
    .meta{display:flex; align-items:center; justify-content:space-between; gap:12px; margin:0 12px 12px; font-size:12px; color:var(--muted)}

    /* Lightbox */
    dialog#lightbox{width:min(1080px, 94vw); border:1px solid rgba(9,33,51,.12); background:color-mix(in oklab, var(--panel), white 20%); border-radius:18px; box-shadow:0 30px 80px rgba(0,0,0,.25)}
    dialog::backdrop{background:rgba(0,0,0,.35)}
    .lightbox-body{position:relative}
    .lightbox-body img, .lightbox-body video{width:100%; height:auto; border-radius:14px}
    .lightbox-caption{display:flex; align-items:center; justify-content:space-between; color:var(--muted); font-size:14px; margin-top:8px}
    .close{position:absolute; top:10px; right:10px; border:none; background:rgba(0,0,0,.65); color:#fff; border-radius:999px; width:40px; height:40px; font-size:18px; cursor:pointer}

    /* Formulaire (clair) */
    input, textarea{background:var(--panel-2) !important; color:var(--text) !important; border:1px solid rgba(9,33,51,.15) !important}

    /* Footer clair */
    footer{border-top:1px solid rgba(9,33,51,.10); color:var(--muted)}

    /* Responsif */
    @media (max-width: 960px){
      .hero-card{grid-template-columns:1fr}
      .card{grid-column: span 6}
    }
    @media (max-width: 600px){
      .nav{gap:10px}
      .navlinks{display:none}
      .card{grid-column: span 12}
      .hero{padding-top:28px}
    }
  </style>
</head>
<body>
  <header>
    <div class="container nav">
      <div class="brand">
        <div class="logo" aria-hidden="true"></div>
        <h1>Mélanie Prunier</h1>
      </div>
      <nav class="navlinks" aria-label="Navigation principale">
        <a class="pill" href="#illustration">Illustration & édition</a>
        <a class="pill" href="#communication">Communication</a>
        <a class="pill" href="#theatre">Théâtre</a>
        <a class="pill" href="#storyboard">Storyboard</a>
        <a class="pill" href="#animation">Animation</a>
        <a class="pill" href="#contact">Contact</a>
      </nav>
    </div>
  </header>

  <main>
    <section class="hero">
      <div class="container">
        <div class="hero-card">
          <div>
            <span class="eyebrow">Direction artistique & estampes</span>
            <h2>Illustratrice · autrice · directrice artistique</h2>
            <p>
              Portfolio multi‑disciplinaire : édition, communication, théâtre, storyboard et animation.
            </p>
            <div class="tags" aria-label="Compétences">
              <span class="tag">Direction artistique</span>
              <span class="tag">Design éditorial</span>
              <span class="tag">Affiche & communication</span>
              <span class="tag">Storyboard</span>
              <span class="tag">Animation 2D/3D</span>
            </div>
            <div class="cta-row">
              <a class="btn primary" href="#contact">Me contacter</a>
              <a class="btn" href="CV.pdf" download>CV (PDF)</a>
            </div>
          </div>
          <div>
            <picture>
              <!-- Remplacez par une image de couverture (1200×900 conseillé) -->
              <img src="assets/cover-placeholder.webp" alt="Aperçu de projets récents" loading="lazy">
            </picture>
          </div>
        </div>
      </div>
    </section>

    <!-- SECTION: Illustration & édition -->
    <section id="illustration" aria-labelledby="illustration-title">
      <div class="container">
        <div class="section-title">
          <h3 id="illustration-title">Illustration & édition</h3>
          <p>Livres, couvertures, presse, expositions.</p>
        </div>
        <div class="grid" data-section="illustration">
          <article class="card">
            <a href="assets/illustration/projet-01.webp" class="lightbox" data-title="Projet édition — 2024" data-info="Livre illustré, 128p, Éditeur X">
              <div class="thumb"><img src="assets/illustration/projet-01-thumb.webp" alt="Projet édition 2024 — couverture" loading="lazy"></div>
              <h4>Projet édition — 2024</h4>
              <p>Livre illustré, 128 pages. Direction artistique & mise en page.</p>
              <div class="meta"><span>Éditeur X</span><span>Edition</span></div>
            </a>
          </article>
          <article class="card">
            <a href="assets/illustration/projet-02.webp" class="lightbox" data-title="Série presse — 2023" data-info="Illustrations éditoriales">
              <div class="thumb"><img src="assets/illustration/projet-02-thumb.webp" alt="Série d'illustrations éditoriales" loading="lazy"></div>
              <h4>Série presse — 2023</h4>
              <p>Couvertures et pleines pages pour la presse culturelle.</p>
              <div class="meta"><span>Magazine Y</span><span>Presse</span></div>
            </a>
          </article>
          <article class="card">
            <a href="assets/illustration/projet-03.webp" class="lightbox" data-title="Affiches expo — 2022" data-info="Identité visuelle d'exposition">
              <div class="thumb"><img src="assets/illustration/projet-03-thumb.webp" alt="Affiches pour exposition" loading="lazy"></div>
              <h4>Affiches expo — 2022</h4>
              <p>Identité visuelle et signalétique pour un cycle d'expositions.</p>
              <div class="meta"><span>Musée Z</span><span>Affiche</span></div>
            </a>
          </article>
        </div>
      </div>
    </section>

    <!-- SECTION: Communication -->
    <section id="communication" aria-labelledby="communication-title">
      <div class="container">
        <div class="section-title">
          <h3 id="communication-title">Communication</h3>
          <p>Identités visuelles, campagnes, affiches, réseaux.</p>
        </div>
        <div class="grid" data-section="communication">
          <article class="card">
            <a href="assets/communication/campagne-01.webp" class="lightbox" data-title="Campagne culturelle — 2024" data-info="Affichage & social">
              <div class="thumb"><img src="assets/communication/campagne-01-thumb.webp" alt="Campagne culturelle 2024" loading="lazy"></div>
              <h4>Campagne culturelle — 2024</h4>
              <p>Concept, direction artistique et déclinaisons print & digital.</p>
              <div class="meta"><span>Ville A</span><span>Campagne</span></div>
            </a>
          </article>
          <article class="card">
            <a href="assets/communication/identite-01.webp" class="lightbox" data-title="Identité visuelle — 2023" data-info="Logo & charte">
              <div class="thumb"><img src="assets/communication/identite-01-thumb.webp" alt="Identité visuelle 2023" loading="lazy"></div>
              <h4>Identité visuelle — 2023</h4>
              <p>Logo, palette, typographies et règles d'usage.</p>
              <div class="meta"><span>Collectif B</span><span>Branding</span></div>
            </a>
          </article>
        </div>
      </div>
    </section>

    <!-- SECTION: Théâtre -->
    <section id="theatre" aria-labelledby="theatre-title">
      <div class="container">
        <div class="section-title">
          <h3 id="theatre-title">Théâtre</h3>
          <p>Affiches, programmes, scénographies, dossiers.</p>
        </div>
        <div class="grid" data-section="theatre">
          <article class="card">
            <a href="assets/theatre/affiche-01.webp" class="lightbox" data-title="Affiche — 2024" data-info="Saison Théâtre C">
              <div class="thumb"><img src="assets/theatre/affiche-01-thumb.webp" alt="Affiche de théâtre 2024" loading="lazy"></div>
              <h4>Affiche — 2024</h4>
              <p>Affiche officielle d'une pièce contemporaine.</p>
              <div class="meta"><span>Théâtre C</span><span>Affiche</span></div>
            </a>
          </article>
        </div>
      </div>
    </section>

    <!-- SECTION: Storyboard -->
    <section id="storyboard" aria-labelledby="storyboard-title">
      <div class="container">
        <div class="section-title">
          <h3 id="storyboard-title">Storyboard</h3>
          <p>Pré‑production pour film, animation, jeu vidéo.</p>
        </div>
        <div class="grid" data-section="storyboard">
          <article class="card">
            <a href="assets/storyboard/board-01.webp" class="lightbox" data-title="Board — Séquence A" data-info="24 vignettes">
              <div class="thumb"><img src="assets/storyboard/board-01-thumb.webp" alt="Storyboard séquence A" loading="lazy"></div>
              <h4>Board — Séquence A</h4>
              <p>Découpage technique et intentions de mise en scène.</p>
              <div class="meta"><span>Film indé</span><span>Pré‑prod</span></div>
            </a>
          </article>
        </div>
      </div>
    </section>

    <!-- SECTION: Animation -->
    <section id="animation" aria-labelledby="animation-title">
      <div class="container">
        <div class="section-title">
          <h3 id="animation-title">Animation</h3>
          <p>Shorts, boucles, character design, animatics.</p>
        </div>
        <div class="grid" data-section="animation">
          <article class="card">
            <a href="assets/animation/loop-01.mp4" class="lightbox" data-title="Loop — 2023" data-info="MP4 1080p">
              <div class="thumb"><img src="assets/animation/loop-01-thumb.webp" alt="Boucle animée 2023" loading="lazy"></div>
              <h4>Loop — 2023</h4>
              <p>Boucle d'animation. Design, rig & comp.</p>
              <div class="meta"><span>Perso</span><span>0:08</span></div>
            </a>
          </article>
        </div>
      </div>
    </section>

    <!-- SECTION: Contact -->
    <section id="contact" aria-labelledby="contact-title">
      <div class="container">
        <div class="section-title">
          <h3 id="contact-title">Contact</h3>
          <p>Un projet ? Écrivez‑moi, je réponds vite.</p>
        </div>
        <div class="grid">
          <div class="card" style="grid-column: span 7; padding:16px">
            <form id="contactForm" action="mailto:votreadresse@mail.com" method="post" enctype="text/plain">
              <label>Votre nom<br>
                <input name="nom" required style="width:100%; padding:12px; margin-top:6px; border-radius:10px;">
              </label>
              <br><br>
              <label>Votre email<br>
                <input name="email" type="email" required style="width:100%; padding:12px; margin-top:6px; border-radius:10px;">
              </label>
              <br><br>
              <label>Message<br>
                <textarea name="message" rows="5" required style="width:100%; padding:12px; margin-top:6px; border-radius:10px;"></textarea>
              </label>
              <br><br>
              <button class="btn primary" type="submit">Envoyer</button>
              <p style="color:var(--muted); font-size:12px; margin-top:8px">Astuce : pour un véritable formulaire (sans client mail), utilisez Formspree, Netlify Forms ou Airtable Forms — il suffit d'adapter l'attribut <code>action</code>.</p>
            </form>
          </div>
          <div class="card" style="grid-column: span 5; padding:16px">
            <h4 style="margin:6px 0 10px; color:var(--ink)">Coordonnées</h4>
            <p style="color:var(--muted)">Email : <a href="mailto:votreadresse@mail.com">votreadresse@mail.com</a><br>
              Instagram : <a href="#">@meli_plum_art</a><br>
              LinkedIn : <a href="#">Mélanie Prunier</a></p>
            <h4 style="margin:12px 0 10px; color:var(--ink)">À propos</h4>
            <p style="color:var(--muted)">Courte bio, 3‑4 lignes. Prix, expositions, clients clés, outils favoris.</p>
          </div>
        </div>
      </div>
    </section>
  </main>

  <footer style="border-top:1px solid rgba(9,33,51,.10); padding:24px 0; color:var(--muted)">
    <div class="container" style="display:flex; align-items:center; justify-content:space-between; gap:12px; flex-wrap:wrap">
      <small>© <span id="year"></span> Mélanie Prunier. Tous droits réservés.</small>
      <small>Site statique — hébergement gratuit (GitHub Pages / Netlify / Vercel). Optimisé pour mobile.</small>
    </div>
  </footer>

  <!-- Lightbox minimaliste + améliorations accessibilité -->
  <dialog id="lightbox" aria-label="Agrandissement visuel">
    <div class="lightbox-body">
      <button class="close" aria-label="Fermer">✕</button>
      <div id="lightbox-media"></div>
      <div class="lightbox-caption">
        <strong id="lightbox-title"></strong>
        <span id="lightbox-info"></span>
      </div>
    </div>
  </dialog>

  <script>
    // Utilitaires
    const $ = (sel, el=document) => el.querySelector(sel);
    const $$ = (sel, el=document) => [...el.querySelectorAll(sel)];

    // Année du footer
    $('#year').textContent = new Date().getFullYear();

    // Lightbox
    const dialog = $('#lightbox');
    const lightboxMedia = $('#lightbox-media');
    const t = $('#lightbox-title');
    const i = $('#lightbox-info');

    function openLightbox(href, title, info){
      lightboxMedia.innerHTML = '';
      if(/\.mp4($|\?)/i.test(href)){
        const video = document.createElement('video');
        video.src = href; video.controls = true; video.autoplay = true; video.playsInline = true; video.style.width='100%';
        lightboxMedia.appendChild(video);
      } else {
        const img = new Image(); img.src = href; img.alt = title || '';
        lightboxMedia.appendChild(img);
      }
      t.textContent = title || '';
      i.textContent = info || '';
      dialog.showModal();
    }

    $$('.lightbox').forEach(a => {
      a.addEventListener('click', e => {
        e.preventDefault();
        openLightbox(a.getAttribute('href'), a.dataset.title, a.dataset.info);
      })
    });

    $('.close').addEventListener('click', () => dialog.close());
    dialog.addEventListener('click', e => { if(e.target === dialog) dialog.close(); });
  </script>
</body>
</html>
